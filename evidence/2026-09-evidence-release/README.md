# Evidence Release 2026-09

This directory is reserved for the dated release snapshot. Add raw JSONL,
`environments.json`, `summary.json`, and `REPORT.md` only after the corresponding
run and validation have completed. `manifest.json` is generated last by
`scripts/build_evidence_release.py`.
