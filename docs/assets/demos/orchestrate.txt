> uv run mklang check examples/map_reduce.mk examples/summarize_doc.mk
OK examples/map_reduce.mk
OK examples/summarize_doc.mk
╭────────────────────────────────────── check ───────────────────────────────────────╮
│ files=2                                                                            │
╰────────────────────────────────────────────────────────────────────────────────────╯
> uv run mklang run examples/map_reduce.mk --provider deepseek --set chunks='["mklang
models an agent as a declarative state machine; the document is the program.","An LLM
is the runtime: each generative state is one produce call, judged by gates.","Gates ar
e natural-language transitions; call and over compose machines into orchestrators."]'
# map_reduce · provider=deepseek · tiers={'fast': 'deepseek-chat', 'balanced': 'deepse
ek-chat', 'reasoning': 'deepseek-reasoner'}
DONE map_reduce · provider deepseek
╭────────────────────────────────────── Result ──────────────────────────────────────╮
│ mklang models an agent as a declarative state machine where the document itself    │
│ serves as the program. An LLM operates as the runtime: each generative state       │
│ corresponds to a single produce call, evaluated by gates that judge their output.  │
│ Gates are natural-language transitions connecting different states, and call and   │
│ over operations compose machines into larger orchestrators.                        │
tokens 978+364 · steps 2
